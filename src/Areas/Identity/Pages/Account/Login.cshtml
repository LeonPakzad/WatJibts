@page
@model LoginModel
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<LoginModel> l

@{
    ViewData["Title"] = @l["log_in"];
}

<h1>@ViewData["Title"]</h1>

<hr />

<div class="form-container">

    <form id="account" method="post">


        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

        <div class="form-field">
            <label class="form-label"   asp-for="Input.Email" >@l["email"]</label>
            <input class="form-control" asp-for="Input.Email" autocomplete="username" aria-required="true" placeholder="name@example.com" />
            <span  class="form-error"   asp-validation-for="Input.Email"></span>
        </div>

        <div class="form-field">
            <label class="form-label"   asp-for="Input.Password">@l["password"]</label>
            <input class="form-control" asp-for="Input.Password" autocomplete="current-password" aria-required="true" placeholder="password" />
            <span  class="form-error"   asp-validation-for="Input.Password"></span>
        </div>

        <div>
            <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">@l["log_in"]</button>
        </div>

        <div>
            <p>
                <a id="forgot-password" asp-page="./ForgotPassword">@l["forgot_password"]</a>
            </p>
            <p>
                <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">@l["register_as_new"]</a>
            </p>
            <p>
                <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">@l["resend_email_confirmation"]</a>
            </p>
        </div>
    </form>
</div>

